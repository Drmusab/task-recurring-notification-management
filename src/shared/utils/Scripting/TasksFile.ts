/**
 * TasksFile stub for scripting/query interfaces.
 * This minimal interface is sufficient for TaskLocation and Link usage.
 */
export interface TasksFile {
  /** Path to the file containing tasks */
  readonly path: string;
}
