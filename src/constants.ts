/**
 * Global constants for the plugin.
 * These were originally intended to be re-exported from the calendar module,
 * but that module doesn't define them. Defined inline as canonical values.
 */

/** Default week format for calendar views */
export const DEFAULT_WEEK_FORMAT = "YYYY-[W]WW";

/** Words per dot in calendar dot indicators */
export const DEFAULT_WORDS_PER_DOT = 250;

/** Calendar view type identifier for SiYuan custom views */
export const VIEW_TYPE_CALENDAR = "calendar";

/** Whether to trigger calendar data load on view open */
export const TRIGGER_ON_OPEN = true;

