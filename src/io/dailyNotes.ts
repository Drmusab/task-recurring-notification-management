/**
 * Daily notes I/O operations stub.
 */
export async function tryToCreateDailyNote(
  _date: unknown,
  _inNewSplit?: boolean,
  _settings?: unknown,
): Promise<void> {
  // Stub: daily note creation not available in SiYuan context
}

export async function createDailyNote(_date: unknown): Promise<unknown> {
  return null;
}
