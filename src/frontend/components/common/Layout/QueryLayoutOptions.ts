/**
 * Layout options for query results display.
 */
export interface QueryLayoutOptions {
  /** Whether to show task count */
  showTaskCount?: boolean;
  /** Whether to show backlinks */
  showBacklinks?: boolean;
  /** Whether to show urgency score */
  showUrgency?: boolean;
}
