/**
 * Styles Layer — Global SCSS & CSS
 *
 * All global styles. Component-scoped styles live alongside their
 * respective .svelte files.
 *
 * ── Files ────────────────────────────────────────────────────
 *   main.scss          — Primary plugin layout & theme
 *   renderer.scss      — SiYuan renderer integration
 *   optimistic-ui.scss — Optimistic update animations
 *   accessibility.css  — Screen reader & focus styles
 *   obsidian-compat.css — Obsidian compatibility shim
 *
 * ── Import Convention ────────────────────────────────────────
 * SCSS/CSS files are imported directly:
 *   import "@styles/main.scss";
 *   import "@styles/accessibility.css";
 */

// This barrel is intentionally empty — styles are imported as side-effects.
// It exists to document the styles directory structure.
export {};
